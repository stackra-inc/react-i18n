/**
 * Contexts Barrel Export
 *
 * - {@link I18nContext} — React context for i18n state
 * - {@link useI18nContext} — Raw context access hook
 *
 * @module contexts
 */

export { I18nContext, useI18nContext } from './i18n.context';
