/**
 * Enums Barrel Export
 *
 * Centralized entry point for all enum definitions used across the i18n plugin.
 *
 * - {@link LocaleResolverPriority} — Priority levels for locale resolver ordering
 *
 * @module enums
 */

export { LocaleResolverPriority } from './locale-resolver-priority.enum';
